'use strict'

var aj = 'kuty'

function addAToEnd(a) {
  return a + 'a';
}

console.log(addAToEnd(aj))
// write a function that gets a string as an input
// and appends an 'a' character to its end and returns a new string
