'use strict';

var j1 = 10;
var j2 = 3;

if (Math.pow(j2, 2) < j1 && Math.pow(j2, 3) > j1) {
  console.log(true);
} else {
  console.log(false);
}
// tell if j1 is higher than j2 squared
// and smaller than j2 cubed
